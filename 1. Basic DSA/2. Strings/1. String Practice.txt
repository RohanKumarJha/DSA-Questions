// input
// charAt(),length(),indexOf() and lastIndexOf()
// compareTo()
// contains()
// startsWith(), endsWith(), toLowerCase(), toUpperCase()
// concat()
// split(" ")
import java.util.*;
class HelloWorld {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the string");
        String str = sc.nextLine();
        String str2 = "Rishabh Jha";
        System.out.println(str);
        for(int i=0; i<str.length(); i++) {
            System.out.println(str.charAt(i));
        }
        System.out.println(str.lastIndexOf('a'));
        System.out.println(str.compareTo(str2));
        System.out.println(str.contains("Jha"));
        System.out.println(str.startsWith("Rohan"));
        System.out.println(str.endsWith("Jha"));
        System.out.println(str.toLowerCase());
        System.out.println(str.toUpperCase());
        System.out.println(str.concat(" Jha Ji"));
    }
}

// String with string/char/int
class HelloWorld {
    public static void main(String[] args) {
        String str = "Rohan";
        char ch = 'R';
        int val1 = 10;
        int val2 = 20;
        System.out.println(str+ch);
        System.out.println(str+str);
        System.out.println(ch+ch);
        System.out.println(str+val1+val2);
        System.out.println(str+(val1+val2));
        System.out.println(val1+val2+str);
    }
}

// substring(i,j) / substring(i)
class HelloWorld {
    public static void main(String[] args) {
        String str = "Rohan Kumar Jha";
        System.out.println(str.substring(3));
        System.out.println(str.substring(3,8));
    }
}

// str.equals(), ==
class HelloWorld {
    public static void main(String[] args) {
        String str1 = "Rohan Kumar Jha";
        String str2 = "Rohan Kumar Jha";
        String str3 = new String("Rohan Kumar Jha");
        System.out.println(str1 == str2);
        System.out.println(str1.equals(str2));
        System.out.println(str1 == str3);
        System.out.println(str1.equals(str3));
    }
}

		// Questions
Given a string s, print all the substrings of s.
Reverse a string.
convert uppercase to lowercase and vice versa...
Strings are palindrome or not.
Count the number of palindromic substrings.
Reverse each word in a sentence.
Compress the given string.

		// Solutions
//1st Question
import java.util.*;
class Substrings {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the string");
        String str = sc.nextLine();
        System.out.println("All the substrings are");
        for(int i=0; i<str.length(); i++) {
            for(int j=0; j<=i; j++) {
                System.out.print(str.charAt(j));
            } System.out.println();
        }
    }
}

// 2nd Question
import java.util.*;
class Substrings {
    public static void reverse(String str) {
        int n = str.length();
        String str2 = "";
        for(int i=0; i<n; i++) {
            char ch = str.charAt(n-i-1);
            str2 = str2+ch;
        }
        System.out.println(str2);
    }
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the string");
        String str = sc.nextLine();
        reverse(str);
    }
}

// 3rd Question
import java.util.*;
class Substrings {
    public static void cases(String str) {
        String str2 = "";
        for(int i=0; i<str.length(); i++) {
            char ch = str.charAt(i);
            if(ch == ' ') {
                str2 += ' ';
                continue;
            }
            int Ascii = (int)ch;
            if(Ascii >= 97) {
                Ascii -= 32;
            } else {
                Ascii += 32;
            }
            str2 = str2 + (char)Ascii;
        } System.out.println(str2);
    }
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the string");
        String str = sc.nextLine();
        cases(str);
    }
}

// 4th Question
import java.util.*;
class Substrings {
    public static void palindrome(String str) {
        int n = str.length();
        str = str.toLowerCase();
        int left=0, right=n-1;
        boolean flag = true;
        while(left<right) {
            if(str.charAt(left) != str.charAt(right)) {
                flag = false;
                break;
            }
            left++;
            right--;
        }
        if(flag == true) {
            System.out.println("String is Palindrome");
        } else {
            System.out.println("String is not Palindrome");
        }
    }
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the string");
        String str = sc.nextLine();
        palindrome(str);
    }
}